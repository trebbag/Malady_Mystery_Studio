# Services area instructions

When implementing services:
- keep service boundaries explicit,
- prefer typed interfaces,
- use in-memory adapters first unless the ticket requires persistence,
- keep orchestrator responsibilities separate from artifact generation logic,
- add service-level README updates when responsibilities change.
