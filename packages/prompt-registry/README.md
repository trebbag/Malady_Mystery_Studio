# packages/prompt-registry

Future home for prompt templates, versioning metadata, and registry helpers.

Goal:
- keep prompt changes reviewable,
- attach prompt versions to workflow artifacts,
- prevent hidden prompt drift.
