# shared-config source

Foundation helpers shared by the validator and starter services:
- repo-root discovery
- schema loading and validation
- HTTP request/response helpers
- identifier generation for starter artifacts
