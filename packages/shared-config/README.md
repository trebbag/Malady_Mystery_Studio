# packages/shared-config

Shared configuration primitives for applications and services.

Current contents:
- repository-root discovery
- JSON-schema registry helpers
- lightweight HTTP helpers for the starter services
- identifier helpers for in-memory starter persistence

Likely future contents:
- environment parsing
- logging defaults
- tracing defaults
- feature flags
