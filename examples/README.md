# Examples

These example artifacts are intentionally small but valid. They exist to:
- clarify intended structure,
- seed early tests,
- make schema evolution easier to review,
- provide Codex with concrete examples of the target artifact shapes.

The examples are not exhaustive or publication-ready.

Naming convention:
- `contracts/foo-bar.schema.json` pairs with `examples/sample_foo_bar.json`
