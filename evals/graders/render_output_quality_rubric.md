# Render Output Quality

- Confirm that a `rendered-asset-manifest` exists for the workflow run.
- Confirm that all required panels are present in the manifest.
- Confirm that rendered assets retain storage locations, checksums, and MIME types.
- Confirm that render execution completed without collapsing lettering back into image generation.
- Fail when pilot export would rely on prompt-only artifacts instead of rendered assets.
