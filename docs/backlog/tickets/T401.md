    # T401: Implement eval harness and dataset loaders

    - **Status:** Proposed
    - **Milestone:** M4
    - **Estimate:** 6–10 hours
    - **Depends on:** T104, T202, T302, T303

    ## Goal

    Make evals runnable and repeatable.

    ## Scope

    - Load JSONL datasets.
- Run seeded checks or adapter graders.
- Store eval results.
- Document how to add cases and thresholds.


    ## Out of scope

    - No external hosted eval platform integration required yet.


    ## Acceptance criteria

    - At least one evaluation pass can run locally.
- Thresholds are loaded from config.
- Results can be attached to a workflow run.


    ## Suggested Codex implementation prompt

    Read:
    - `AGENTS.md`
    - `docs/master-spec.md`
    - `docs/repo-map.md`
    - this ticket file
    - all affected files in `contracts/`, `evals/`, and `docs/adr/`

    Then:
    1. summarize the smallest safe implementation plan,
    2. list affected files before editing,
    3. implement only this ticket,
    4. update tests/evals/docs in the same change,
    5. explain any follow-on tasks that should remain separate.
