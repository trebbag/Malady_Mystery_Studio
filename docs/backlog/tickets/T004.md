    # T004: Define the workflow state machine and event model

    - **Status:** Proposed
    - **Milestone:** M0
    - **Estimate:** 4–6 hours
    - **Depends on:** T001, T003

    ## Goal

    Encode the end-to-end artifact lifecycle as explicit states and events.

    ## Scope

    - Define workflow stages and transitions.
- Define status enums and event payload shape.
- Add a machine-readable workflow spec.
- Add tests for invalid transitions.


    ## Out of scope

    - No production workflow engine implementation yet.


    ## Acceptance criteria

    - Valid transitions are accepted.
- Invalid transitions are rejected.
- State names line up with `workflow-run` schema and docs.


    ## Suggested Codex implementation prompt

    Read:
    - `AGENTS.md`
    - `docs/master-spec.md`
    - `docs/repo-map.md`
    - this ticket file
    - all affected files in `contracts/`, `evals/`, and `docs/adr/`

    Then:
    1. summarize the smallest safe implementation plan,
    2. list affected files before editing,
    3. implement only this ticket,
    4. update tests/evals/docs in the same change,
    5. explain any follow-on tasks that should remain separate.
