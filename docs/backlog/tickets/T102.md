    # T102: Implement disease canonicalization service interface

    - **Status:** Proposed
    - **Milestone:** M1
    - **Estimate:** 4–8 hours
    - **Depends on:** T101

    ## Goal

    Normalize disease input into a canonical condition record.

    ## Scope

    - Define canonicalization interface.
- Return normalized disease names and aliases.
- Support confidence scoring and unresolved status.
- Document future ontology integration points.


    ## Out of scope

    - No production ontology integration required yet.


    ## Acceptance criteria

    - Known test inputs normalize predictably.
- Ambiguous input can be flagged for review.
- Output matches the contract expectations.


    ## Suggested Codex implementation prompt

    Read:
    - `AGENTS.md`
    - `docs/master-spec.md`
    - `docs/repo-map.md`
    - this ticket file
    - all affected files in `contracts/`, `evals/`, and `docs/adr/`

    Then:
    1. summarize the smallest safe implementation plan,
    2. list affected files before editing,
    3. implement only this ticket,
    4. update tests/evals/docs in the same change,
    5. explain any follow-on tasks that should remain separate.
