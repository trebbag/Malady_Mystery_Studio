    # T003: Implement contract validation tooling

    - **Status:** Proposed
    - **Milestone:** M0
    - **Estimate:** 2–4 hours
    - **Depends on:** T001

    ## Goal

    Make schemas a first-class executable asset.

    ## Scope

    - Add programmatic schema loading.
- Validate example artifacts against schemas.
- Add a regression test for schema/example drift.
- Document how to add new schemas.


    ## Out of scope

    - No database changes.


    ## Acceptance criteria

    - A failing example causes the validation step to fail.
- Schema loading supports all current JSON files in `contracts/`.
- Docs explain how to extend the system.


    ## Suggested Codex implementation prompt

    Read:
    - `AGENTS.md`
    - `docs/master-spec.md`
    - `docs/repo-map.md`
    - this ticket file
    - all affected files in `contracts/`, `evals/`, and `docs/adr/`

    Then:
    1. summarize the smallest safe implementation plan,
    2. list affected files before editing,
    3. implement only this ticket,
    4. update tests/evals/docs in the same change,
    5. explain any follow-on tasks that should remain separate.
