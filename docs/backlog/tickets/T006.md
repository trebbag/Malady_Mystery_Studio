    # T006: Create project and workflow-run API skeleton

    - **Status:** Proposed
    - **Milestone:** M0
    - **Estimate:** 4–6 hours
    - **Depends on:** T001, T004, T005

    ## Goal

    Create the first thin API slice for creating a project and starting a workflow run.

    ## Scope

    - Define handlers for create project and start run.
- Return typed payloads that match the contracts.
- Use in-memory persistence adapters only.
- Add integration tests.


    ## Out of scope

    - No real auth enforcement yet.
- No asynchronous execution yet.


    ## Acceptance criteria

    - API starts and passes smoke tests.
- Create-run flow persists and can be read back.
- Responses conform to schema.


    ## Suggested Codex implementation prompt

    Read:
    - `AGENTS.md`
    - `docs/master-spec.md`
    - `docs/repo-map.md`
    - this ticket file
    - all affected files in `contracts/`, `evals/`, and `docs/adr/`

    Then:
    1. summarize the smallest safe implementation plan,
    2. list affected files before editing,
    3. implement only this ticket,
    4. update tests/evals/docs in the same change,
    5. explain any follow-on tasks that should remain separate.
