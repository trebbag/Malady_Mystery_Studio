    # T402: Implement release bundle assembly and export

    - **Status:** Proposed
    - **Milestone:** M4
    - **Estimate:** 6–10 hours
    - **Depends on:** T304, T401

    ## Goal

    Assemble all approved artifacts into a typed release bundle.

    ## Scope

    - Collect artifact references.
- Attach approvals, scores, and export metadata.
- Support JSON export and a human-readable bundle index.
- Add validation and tests.


    ## Out of scope

    - No production publishing destination integration yet.


    ## Acceptance criteria

    - Release bundle validates.
- Exports contain required artifacts and approvals.
- A run can be promoted from approved to exported.


    ## Suggested Codex implementation prompt

    Read:
    - `AGENTS.md`
    - `docs/master-spec.md`
    - `docs/repo-map.md`
    - this ticket file
    - all affected files in `contracts/`, `evals/`, and `docs/adr/`

    Then:
    1. summarize the smallest safe implementation plan,
    2. list affected files before editing,
    3. implement only this ticket,
    4. update tests/evals/docs in the same change,
    5. explain any follow-on tasks that should remain separate.
