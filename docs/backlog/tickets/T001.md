    # T001: Scaffold the monorepo foundation

    - **Status:** Proposed
    - **Milestone:** M0
    - **Estimate:** 3–5 hours
    - **Depends on:** None

    ## Goal

    Create the initial repository structure, root config files, and placeholder package/app/service directories.

    ## Scope

    - Create root workspace files.
- Create `apps/`, `services/`, `packages/`, `docs/`, `contracts/`, `evals/`, `api/`, `scripts/`.
- Add readmes to every top-level working area.
- Do not implement business logic yet.


    ## Out of scope

    - No production framework wiring beyond the minimal repo scaffold.
- No real database or queue yet.


    ## Acceptance criteria

    - `pnpm-workspace.yaml` exists.
- Root README and repo map are coherent.
- Directory structure matches the pack design.
- Validation script still passes.


    ## Suggested Codex implementation prompt

    Read:
    - `AGENTS.md`
    - `docs/master-spec.md`
    - `docs/repo-map.md`
    - this ticket file
    - all affected files in `contracts/`, `evals/`, and `docs/adr/`

    Then:
    1. summarize the smallest safe implementation plan,
    2. list affected files before editing,
    3. implement only this ticket,
    4. update tests/evals/docs in the same change,
    5. explain any follow-on tasks that should remain separate.
